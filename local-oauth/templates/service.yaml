apiVersion: v1
kind: Service
metadata:
  name: {{ include "local-oauth.fullname" . }}
  labels:
    {{- include "local-oauth.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "local-oauth.selectorLabels" . | nindent 4 }}
